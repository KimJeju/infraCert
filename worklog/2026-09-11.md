# 2026-09-11 — [CC] KISA 2026 상세가이드 구조화 (플랫폼/제품별 YAML·JSON)

## 요약
- 형 요청: "가이드 항목을 yaml/xml/json 으로, 리눅스·레드햇·apache 등 분류로 쪼개서".
- 873쪽 PDF 전체를 파싱해 **380 항목**(번호 항목 359 + 웹앱 21)을 12 섹션 × 플랫폼별 YAML 52파일 + `all.json` 으로 냈다.
- 생성기 `scripts/extract_guide.py` 재실행 가능. 회귀 테스트 3건 추가(총 166).
- 산출물 `rulepacks/kisa-2026/guide/` 는 **미커밋** — 공개 저장소에 가이드 전문(全文) 파생물을 올릴지 형 판단. 🔴

## 한 일

### 1. 가이드 구조 분석
- 항목 헤더 `U-01(상) UNIX > 1. 계정 관리<이름>개요…`, 필드 라벨 점검내용/목적/위협/참고/대상/판단기준(양호·취약)/조치/영향/사례.
- 사례는 글머리 기호가 `l` 로 추출됨: `lSOLARIS[Telnet]Step 1)…`, `lLINUX[Redhat]…[Debian]…`, `lApache`, `lCisco IOS`, `lOracle DB` …
- 섹션 12개(Unix 67 · Windows 64 · 웹서비스 26 · 보안장비 23 · 네트워크 38 · 제어시스템 50 · PC 18 · DBMS 26 · 이동통신 4 · 웹앱 21 · 가상화 24 · 클라우드 19).

### 2. 추출기 `scripts/extract_guide.py`
- pypdf 텍스트 → 페이지 머리글 제거 → 헤더 정규식으로 항목 분할 → 라벨 위치로 필드 절단(표 순서가 뒤섞여도 복구) → 플랫폼 불릿 화이트리스트로 사례 분할 → `[태그]Step 1)` 변형(Telnet/SSH, Redhat/Debian, 메일서버별) 분리 → `Step N)` 리스트.
- 라벨 오탐 가드: 본문 속 `대상 시스템`, `참고하여`, 웹앱 사례의 `- 조치 방법`, 표 머리글 `구분 상세 설명`.
- 플랫폼 불릿 뒤 주석 허용(`SOLARIS(5.9 이하 버전)`, `NT(IIS 4.0)`), 웹앱·보안장비의 기법 불릿(`lLDAP 인젝션- 점검 방법`)은 common 으로.
- 가이드 오타 1건 보정: 웹앱 "자동화 공격" 본문 헤더 `CC`(쿠키 변조와 중복) → 항목표대로 `AU`.

### 3. 산출 구조
`<섹션>/<플랫폼>.yaml`(그 플랫폼 절차만) + `<섹션>/_all.yaml` + `all.json` + `index.yaml` + README.
unix: solaris/linux/linux-redhat/linux-debian/aix/hpux · web-service: apache/nginx/iis/tomcat/jeus/webtob · network: cisco-ios/juniper-junos/radware-alteon/passport/piolink-plos · dbms: oracle/mssql/mysql/postgresql/tibero/altibase/cubrid · virtualization: vmware-esxi/vcenter/xenserver/kvm/nutanix.

### 4. 한계(README 에 기록)
- PDF 셀 줄바꿈 소실로 설정값이 붙어 나옴(`PASS_MAX_DAYS 90PASS_MIN_DAYS 0`). 명령으로 그대로 쓰지 말 것.
- **레드햇/데비안 분리는 2건뿐**(U-02, U-13) — 가이드가 LINUX 를 배포판으로 나눈 항목이 그만큼이다.
- 이동통신 4건은 원문이 판단기준 없는 서술형. W-45 는 사례 없음. 제어시스템 일부는 표 순서 뒤섞임.
- XML 은 안 만들었다 — 필요하면 `all.json` 에서 한 줄 변환.

## 🔴 형 확인필요 → 세션 2 에서 해소
1. ~~`rulepacks/kisa-2026/guide/` 커밋 여부~~ → **미커밋, `.gitignore`**(형 결정 "파생물 생략").
2. ~~(a)(b)(c) 활용~~ → **셋 다 구현**(아래 세션 2).

## 세션 2 — [CC] 가이드 조인(a/b/c) + 부분 룰팩 빌드·가져오기

형 결정: (a)(b)(c) 다 하고, 가이드 파생물은 저장소에 넣지 않는다. 컨설턴트가 자산에 맞게 룰팩을 만들어 툴에 업로드.

### 한 일
- **(a) 룰 조치방법 채움** — `scripts/enrich_rules.py`: 가이드 `all.json` 을 id 로 조인해 `rules/*.yaml` 의 `remediation`(61) 과 manifest `rules:` 메타의 `remediation`(110, D-/WEB- 포함)을 채움. 주석 보존(텍스트 덧붙임). `note` 는 안 건드림(증적 끝에 붙는 한 줄이라). regen_manifest 가 YAML `remediation` 을 메타로 옮긴다.
- **(b) 리포트** — XLSX 결과 시트·HTML 에 `조치방법` 컬럼. 취약·수동확인 행에만 붙고 양호 행은 빈칸(가이드 있으면 가이드, 없으면 manifest 메타).
- **(c) 수동확인 워크벤치** — 수집근거 아래 "가이드" 패널: 양호/취약 기준 · 조치 · 조치 시 영향 · 플랫폼별 Step. 가이드 없으면 메타 조치방법만, 그것도 없으면 "가이드 없음" 명시.
- **로더** — 룰팩 `guide/**/*.yaml` 을 `pack.guide` 로 읽음(데이터라 해시 검증 없음, YAML 깨져도 실행은 안 막음). `remediation_map()`.
- **부분 룰팩 빌더** `scripts/build_rulepack.py` — 프로파일/룰/번들 선택 → sha 재계산 → 스크립트·동반파일·rules yaml·선택 가이드 항목만 zip.
- **가져오기** `rulepack/importer.py` + 룰팩 탭 "룰팩 가져오기(.zip)" + 팩 전환 콤보(config `rulepack`). zip 전 멤버 검사(절대경로·`..`·심볼릭링크 → 전부 거부), manifest 위치로 접두 결정, 이름은 manifest `name`. 풀고 나서 로더 검사 그대로.
- `.gitignore`: `/rulepacks/*/guide/`. 레포 위생 테스트는 guide/ 를 의도된 예외로.
- 테스트 +5 (경로탈출 거부·manifest 없음 거부·빌드→가져오기→로드 왕복·메타 조치 존재·리포트 컬럼). 총 171.
- 샘플: `build_rulepack.py --profile aix-server --rules U-01,U-16,U-18 --guide …` → 5파일 283 KB(가이드 67항목 포함).

### 남김
- 가져온 룰팩은 `rulepacks/` (exe 옆) 에 남는다 — 반출 대상 폴더라 무잔류 원칙과 충돌 없음. 지우는 버튼은 없음(탐색기로).

## 세션 3 — [CC] 룰팩 탭 "선택 항목으로 룰팩 zip 만들기" 버튼

- 빌드 로직을 `rulepack/builder.py` 로 옮김(CLI `scripts/build_rulepack.py` 는 래퍼). 빈 선택은 `BuildError`.
- 룰팩 탭 프로파일 줄에 버튼: 체크한 번들·룰 → 이름 입력(기본 `<팩>-subset`) → 저장 위치 → zip. 가이드는 현재 팩의 `guide/` 에서 해당 항목만 딸려간다.
- 테스트 +2 (버튼이 체크 상태 그대로 emit, 빈 선택 거부). 총 173.

## 세션 4 — [CC] 형 지시 "1~5 다 해": Windows·Oracle·Cisco 네이티브 + 잔손질 (재빌드는 맨 뒤)

### 엔진 일반화
- `RemoteEnvironment.params` ← `HostJob.params` ← 호스트 파라미터. 선언형 룰 `shell: sh|powershell|raw`; sh 룰은 `env K=V` 접두(validate_env). 출력 CRLF 정규화(값 끝 `\r` 로 `eq` 오탐 잡음 — W-60 이 그렇게 잡혔다).
- 전송 3종 신규: `transport/local.py`(이 PC, 테스트·PC 점검), `transport/winrm.py`(pywinrm NTLM, 비밀번호만, 파일전송 없음), `transport/netdev.py`(invoke_shell, `show version` 으로 벤더 판별 후 그 벤더 페이징 해제만, running-config 캐시, `% Invalid` 를 실패로).
- `build_connection` 이 플랫폼으로 전송 선택: windows/pc→WinRM(22면 5985), network→장비 셸, 그 외 SSH.

### 룰
- **W-01~64** `scripts/gen_rules_windows.py` — 레지스트리/서비스/net accounts/icacls 조회. secedit 는 파일을 쓰므로 안 씀 → 사용자 권한 할당류는 MANUAL. **이 PC 에서 64개 실행: 예외 0**, GOOD 27 · VULN 17 · MANUAL 16 · NA 4. 한/영 `net accounts` 라벨 둘 다 처리.
- **D-01~26** `scripts/gen_rules_oracle.py` — SSH→`sqlplus -S / as sysdba` SELECT. ORA-/SP2- 는 MANUAL. MSSQL 전용(D-16/23/24)·ODBC(D-13) NA. 실 DB 없음 → canned 테스트만.
- **N-01~38** `scripts/gen_rules_netdev.py` — Cisco IOS `show running-config` 정규식. IOS 기본값(bootp/source-route/pad/cdp 기본 활성 등) 반영. Junos 는 SKIPPED. 가짜 채널 테스트로 프롬프트·캐시·에코제거 검증.
- manifest: native 195, rules 221, 프로파일 windows-native/oracle-native/cisco-native 추가. `regen_manifest` 가 `*-native` 프로파일을 `platforms`·`prefix` 로 필터(전엔 전부 넣었음).
- `enrich_rules` 로 새 룰 128개 remediation 채움.

### 잔손질(5)
- `rulepack/guide.py` 공용 포맷. 룰팩 탭 항목 클릭 시 가이드 표시. "룰팩 삭제" 버튼(rulepacks/ 직속만, 확인 후 rmtree). 리포트 `판단기준` 컬럼(가이드 양호/취약).

### 테스트
- +29 (`test_platform_rules.py`: Cisco bad/good 21쌍, netdev 세션, Oracle canned, Windows canned, 로컬 PowerShell 실행, 전송 선택). 총 202.

### 다음
- 재빌드(build_exe 에 pywinrm·새 transport hidden import) + 리허설.

### 재빌드 + 리허설 (세션 4 마무리)
- `scripts/build_exe.py` hidden import 추가(winrm·requests_ntlm·spnego — transport/winrm.py 가 lazy import). `--clean` 빌드 116 MB / 477 파일, rulepacks 189 룰 파일 동봉.
- PYZ 아카이브에 infraguard.transport.{local,winrm,netdev}·winrm(10개 모듈)·requests_ntlm·spnego 포함 확인(폴더가 아니라 PYZ 안이라 Test-Path 로는 안 보임).
- 리허설: dist 를 스크래치 폴더로 복사 → exe 실행 → 10초 후 생존, workspace/(artifacts·exports·logs·tmp·assets.db·results.db) 생성, crash.log 없음 → 종료.
- 실제 WinRM/장비/Oracle 접속은 대상이 없어 미검증 — 룰·전송은 가짜 연결·로컬 PowerShell 로만.

## 🔴 형 확인필요
1. 실 대상: AIX 9.3 · Oracle 19c · Windows 서버(WinRM 5985 열려 있어야) · Cisco 장비 — 접속 정보 주면 그 자리에서 오탐 보정.
2. 코드 서명 인증서(반입 심사).
3. dist/ 반입 전 guide/ 동봉 여부(지금은 로컬 guide/ 가 rulepacks 에 같이 복사됨 — 수동확인 사례 표시용. 빼려면 build 에서 제외).

## 세션 5 — [CC] "너가 해줄 순 없니?" → 실장비 없이 갈 수 있는 데까지

- **Cisco 전송 실접속 E2E**: paramiko 서버로 가짜 IOS CLI 를 띄워(`tests/failure/test_netdev_ssh_e2e.py`) `NetdevConnection.connect()` 전 경로를 실제 SSH 로 태움 — 비밀번호 인증, 호스트키 승인 콜백(거부 시 명령 0건), invoke_shell, 배너/프롬프트 대기, 벤더 판별 후 `terminal length 0` 만 전송, `--More--` 없는 설정 수신, 캐시(38룰이 running-config 1회), `% Invalid` 처리, probe(hostname 은 프롬프트에서 — 이 테스트가 None 을 잡아 고침). 38룰 판정 결과 기대치 일치.
- **WinRM 모킹 E2E**: `winrm.Session` 대역으로 URL/NTLM/TLS 검증 옵션·프렐류드·cp949 디코딩·probe 파싱·64룰 실행(예외 0) 확인. 실서버 미접속이라 NTLM 협상 자체는 미검증.
- `scripts/sign_exe.ps1`: 조직 인증서 지문으로 signtool(없으면 Set-AuthenticodeSignature) 서명 + 타임스탬프. `-SelfSignedForTest` 는 파이프라인 확인용.
- `build_exe.py --no-guide`: 반입본에서 가이드 파생물 제외 옵션.
- 테스트 209.

### 못 한 것 · 왜
- **Oracle 실검증**: 이 PC 와 WSL 어디에도 docker/podman 없음. 길은 둘 — WSL 에 docker.io 설치(apt ~100MB) 후 `gvenzl/oracle-free` 이미지(~1.5GB) pull, 또는 Oracle XE 21c RPM(~2.4GB) 직접 설치. 둘 다 대용량 다운로드라 형 승인 필요. 🔴
- **WinRM 실접속**: 이 PC 에 WinRM 을 켜는 건 시스템 설정 변경이라 내가 안 함. 형이 `Enable-PSRemoting` 켜 주면 localhost:5985 로 64룰 실검증 가능.
- **AIX**: 대체 불가.
