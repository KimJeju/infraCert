# 2026-09-10 — [C] 전날 CC 작업 리뷰 (설계자 검증)

## 요약
- CC 의 9/9 작업(U-1~U-9, 룰팩, 네이티브 룰, WSL E2E)을 코드로 직접 검증했다. **설계 불변식 4종 전부 유지**됐다.
- **터미널 비밀번호 마스킹에 구멍 1건 확인.** 붙여넣기·초고속 입력 시 평문이 로그에 남는다. 재현 코드 있음. 🔴
- 공개 저장소 관련 2건: 고객 스크립트가 `.gitignore` 에 없어 `git add -A` 한 번이면 유출 — **[C] 가 선조치**. worklog 의 고객 프로젝트명 노출은 형 판단 필요. 🔴

## 한 일

### 1. 설계 불변식 정적 검증 (src 44파일 / 6,669줄 전수)
| 불변식 | 결과 |
|---|---|
| `decide()` 외부에서 Status 생성 금지 | **위반 0건** |
| `Secret.reveal()` 은 transport 계층 전용 | `transport/ssh.py` 2곳만 (`_auth_kwargs`) |
| 폐쇄망 — 아웃바운드 호출 금지 | **0건** |
| `workspace/` 밖 쓰기 | `config.json`·`rulepacks/profiles`·사용자 지정 내보내기 경로뿐 (명세 허용 범위) |

`native_runner.py` 가 신규 경로임에도 예외·플랫폼불일치·미등록룰을 전부 `error_result()` → `decide()` 로 넘긴 점 확인. B안 도입으로 판정 경로가 갈라지지 않았다.

### 2. git 상태 확인
- 원격 `github.com/KimJeju/infraCert.git` (public), 커밋 2개, **push 완료**(local==origin/main).
- 인덱스 정식 파싱: 86개 엔트리. 소스·테스트·docs·worklog 전체 커밋됨. **고객 스크립트 4개는 미포함** — 여기까지는 정상.
- 다만 미포함 이유가 `.gitignore` 도 `.git/info/exclude` 도 아닌 **"수동으로 add 안 함"** 이었다. `git add -A` 한 번이면 public 으로 나간다.
- → **[C] 선조치**: `.gitignore` 에 `rulepacks/*/scripts/` 추가·커밋 대기 상태로 파일 반영함.

### 3. 터미널 마스킹 검증 — 구멍 발견
`audit/terminal_recorder.py` 의 2중 장치(프롬프트 패턴 + 에코 부재) 중 **에코 부재 판정이 무력화되는 조건**이 있다.

원인: `_finish_line()` 이 Enter 시점에 `stale = any(now - t > ECHO_GRACE ...)` 로 즉시 판정한다.
입력이 한 덩어리로 들어오면 어떤 문자도 아직 "오래되지 않아" `stale=False` → 프롬프트 패턴만이 유일한 방어가 된다.

재현 (프롬프트 패턴 미매칭 상황):

| 시나리오 | 결과 |
|---|---|
| 붙여넣기 후 즉시 Enter | **평문 유출** `[IN] Hunter2!Secret` |
| 개행 포함 한 덩어리 붙여넣기 | **평문 유출** |
| 총 0.07초 초고속 타이핑 | **평문 유출** |
| 짧은 비번 `abc123` 빠르게 | **평문 유출** |
| (참고) 표준 sudo 프롬프트 | 마스킹 OK — 패턴이 방어 |

비밀번호 관리자에서 복사·붙여넣기는 현장에서 흔하다. 실사용 경로다.

기존 `tests/failure/test_terminal_recorder.py` 는 무에코 케이스를 전부 `dt=0.1` 로 잡아 시간이 흐르게 해놓아 이 구멍을 못 덮었다.

**수정 방향 — Enter 시점 즉시판정을 버리고 지연판정으로.**

```python
# 1) 상태 추가
self._deferred: tuple[str, float] | None = None   # (line, t_enter)
self._deferred_secret_hint = False

# 2) _finish_line(): pending 이 비었으면(=이미 다 에코됨) 즉시 평문 기록.
#    남아 있으면 판정을 미룬다.
def _finish_line(self, now):
    if self._secret_by_prompt:
        self._write("[IN] ***"); self.masked_inputs += 1
    elif not self._pending:
        if self._line:
            self._write("[IN] " + (mask(self._line) or ""))
    else:
        self._deferred = (self._line, now)      # 에코가 오는지 더 지켜본다
    self._line = ""
    self._secret_by_prompt = False
    self._tail = ""
    # 주의: _pending 은 비우지 않는다(에코 대조에 계속 쓴다)

# 3) on_output() 끝에서 호출
def _resolve_deferred(self):
    if self._deferred is None:
        return
    line, t0 = self._deferred
    if not self._pending:                       # 에코가 돌아왔다 → 평범한 명령
        self._deferred = None
        if line:
            self._write("[IN] " + (mask(line) or ""))
    elif self._clock() - t0 > ECHO_GRACE:       # 끝내 에코 없음 → 비밀
        self._deferred = None
        self._pending.clear()
        self._write("[IN] ***"); self.masked_inputs += 1

# 4) close(): 미해결 deferred 가 남아 있으면 '***' 로 확정(보수적)
```

이렇게 하면 붙여넣은 **비밀번호**는 끝내 에코가 없어 `***`, 붙여넣은 **명령어**는 에코가 돌아와 평문으로 남는다. 감사 가치를 잃지 않으면서 구멍이 막힌다.

**추가 권고 (같은 파일)**
- `PROMPT_PATTERNS` 에 `>`·`?` 종결 프롬프트와 PIN/passcode/OTP/인증번호 추가. 장비 CLI·Oracle 계열에서 `:` 로 안 끝나는 프롬프트가 흔하다.
- `on_output()` 의 `and self._tail.rstrip().endswith(":")` 조건은 정규식이 이미 `:\s*$` 를 요구하므로 중복이고, `>` 종결 프롬프트를 원천 차단한다. 제거 권고.

**추가할 회귀 테스트** (`tests/failure/test_terminal_recorder.py`)

```python
def test_pasted_password_is_masked(tmp_path):
    """비밀번호 관리자에서 붙여넣기 — 시간이 흐르지 않아도 마스킹돼야 한다."""
    r, clk = _rec(tmp_path)
    r.on_output(b"Enter your PIN> ")          # 패턴에 없는 프롬프트
    r.on_input(b"Hunter2!Secret")             # 한 덩어리
    r.on_input(b"\r")
    r.on_output(b"\r\nAccess granted\r\n")    # 에코 없음
    r.close()
    log = (tmp_path / "t.log").read_text(encoding="utf-8")
    assert "Hunter2!Secret" not in log
    assert "[IN] ***" in log

def test_pasted_command_is_still_recorded(tmp_path):
    """붙여넣은 '명령어' 는 에코가 돌아오므로 평문으로 남아야 한다(감사 가치 유지)."""
    r, clk = _rec(tmp_path)
    r.on_output(b"igtest@web01:~$ ")
    r.on_input(b"ls -la /etc")
    r.on_output(b"ls -la /etc")               # 에코
    r.on_input(b"\r")
    r.on_output(b"\r\ntotal 1\r\n")
    r.close()
    log = (tmp_path / "t.log").read_text(encoding="utf-8")
    assert "[IN] ls -la /etc" in log
```

## 열린 항목 / 다음
- [CC] 위 터미널 지연판정 수정 + 회귀 테스트 2건 추가. **최우선**.
- [CC] `.gitignore` 변경분 커밋(내용은 [C] 가 이미 파일에 반영).
- 나머지 기존 계획(U-10 패키징, 네이티브 룰 확장, WinRM/netmiko/DB)은 그대로.

## 세션 2 — [CC] 리뷰 반영

### 한 일
- **터미널 마스킹 지연판정** (`audit/terminal_recorder.py`): Enter 즉시판정 폐기. 에코 미확인 문자가 남으면 `_deferred` 로 보류 → 이후 출력에서 에코가 돌아오면 평문(명령어), `ECHO_GRACE` 초과·`close()` 시 `***`(비밀). 다음 입력 시작 시에도 보류분 정리. 백스페이스가 pending 도 되감도록 수정.
- 프롬프트 패턴 확장: `:`뿐 아니라 `>`·`?` 종결, PIN/OTP/passcode/인증번호·인증코드. 중복이던 `endswith(":")` 조건 제거.
- 회귀 테스트 4건 추가(붙여넣기 비번 마스킹 / 붙여넣기 명령 평문 유지 / Enter 뒤 늦은 에코 / `>`·PIN 프롬프트). 전체 **100 통과**.
- [C] 선조치분(`.gitignore` `rulepacks/*/scripts/`, README 색인) 검토 후 함께 커밋.
- `2026-09-09.md` 의 고객 경로명 익명화(파일만. **이력엔 남음** — 🔴 1 참조).

### 남김
- 🔴 2(igtest)·3(Q-2 정본)은 그대로 형 판단.

### [형] 결정 (11:15)
- 스크립트는 전부 자작이라 공개 OK → `.gitignore` 잠금 해제, `rulepacks/kisa-2026/scripts/` 4개 커밋. 🔴 1(경로명 이력)도 같은 이유로 종결.
- 룰을 선언형(XML/JSON)으로 만드는 안 제기 → [CC] 검토: YAML/JSON 하이브리드 권고(세션3 예정).

## 🔴 형 확인필요
1. **worklog 가 public 저장소에 올라가 있다.** `2026-09-09.md` 27행에 `cowork/korea_bank_infra_scripts` 경로가 그대로 있다 — 고객 도메인이 읽힌다. 셋 중 선택 필요:
   - 저장소를 private 으로 전환 (가장 간단)
   - worklog 를 `.gitignore` 처리하고 이미 올라간 이력 정리(force push / 히스토리 재작성)
   - 경로명만 익명화(`<internal>/infra_scripts`)하고 유지
   경로만 지워도 **커밋 이력에는 남는다.** private 전환이 아니면 히스토리 정리까지 해야 실효가 있다.
2. WSL 테스트 계정 `igtest` + sshd :2222 존치 여부 (전날 항목, 미결).
3. Q-2 **정본 스크립트 확정** — 아직 미결. 현재 룰팩은 aix/oracle/web 3종만 담고 있어 당장은 안 막히지만, 룰팩을 U-01~U-67 전체로 넓히기 전에 필요.
